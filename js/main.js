"use strict";const menuButton=document.querySelector("#menuButton");const siteNav=document.querySelector("#siteNav");if(menuButton && siteNav){menuButton.addEventListener("click",()=>{const open=siteNav.classList.toggle("is-open");menuButton.setAttribute("aria-expanded",String(open));});siteNav.addEventListener("click",event=>{if(event.target instanceof HTMLAnchorElement){siteNav.classList.remove("is-open");menuButton.setAttribute("aria-expanded","false");}});window.addEventListener("resize",()=>{if(window.innerWidth>860){siteNav.classList.remove("is-open");menuButton.setAttribute("aria-expanded","false");}});}const year=document.querySelector("#year");if(year){year.textContent=String(new Date().getFullYear());}const uniformSection=document.querySelector("#uniformSection");const uniformPanels=document.querySelectorAll("[data-uniform-panel]");if(uniformSection && uniformPanels.length){const showUniform=value=>{uniformPanels.forEach(panel=>{panel.hidden=panel.dataset.uniformPanel !==value;});};uniformSection.addEventListener("change",()=>{showUniform(uniformSection.value);});showUniform(uniformSection.value);}